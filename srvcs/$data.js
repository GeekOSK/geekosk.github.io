angular.module('GeekOSK').service('$data',
    ['$rootScope', '$firebaseArray',
        function($rootScope, $firebaseArray){
            var self = this;
            self.getProjects = function(){

            }
        }
    ]
);
